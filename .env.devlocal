# Configuración para desarrollo local
# Ejecuta el proyecto con: pnpm run dev:local
VITE_API_URL=/api
# Cambia el puerto 3000 si tu backend local corre en otro puerto (ej: 8080, 5000)
VITE_API_TARGET=http://localhost:3000
